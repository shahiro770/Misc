#include <stdio.h>
#include <unistd.h>

int main (int argc, char *argv[]) {
	FILE *fp = NULL;
	FILE *fp2 = fopen("shahir.txt", "w+");

	while(1){
		//fflush(fp);
		printf("No discrimination!");
		fwrite()
		fflush(fp);
		//printf("No discrimination!\n");
		sleep(1);
	}
}